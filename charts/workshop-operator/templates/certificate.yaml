{{- if .Values.webhook.enabled }}

{{- if not (.Capabilities.APIVersions.Has "cert-manager.io/v1") -}}
    {{- fail "cert-manager is not installed" }}
{{ end }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "workshop-operator.fullname" . }}-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "workshop-operator.fullname" . }}-serving-cert
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
  - "{{ include "workshop-operator.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc"
  - "{{ include "workshop-operator.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.cluster.local"
  issuerRef:
    kind: Issuer
    name: {{ include "workshop-operator.fullname" . }}-selfsigned-issuer
  secretName:  webhook-server-cert
{{- end -}}